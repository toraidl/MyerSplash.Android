<?xml version="1.0" encoding="utf-8"?>
<com.juniperphoton.myersplash.widget.item.PhotoFooterView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/row_footer_rl"
    android:layout_width="match_parent"
    android:layout_height="@dimen/footer_height"
    android:gravity="center"
    android:orientation="horizontal">

    <ProgressBar
        android:id="@+id/progress_view"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:indeterminate="true"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintHorizontal_chainStyle="packed"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toLeftOf="@id/footer_hint"
        app:layout_constraintTop_toTopOf="parent"/>

    <TextView
        android:id="@+id/footer_hint"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="12dp"
        android:text="@string/loading"
        android:textColor="@android:color/white"
        app:layout_constraintBottom_toBottomOf="@id/progress_view"
        app:layout_constraintLeft_toRightOf="@id/progress_view"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="@id/progress_view"/>
</com.juniperphoton.myersplash.widget.item.PhotoFooterView>

